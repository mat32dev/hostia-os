# Host.ia Edge — runtime dependencies (kept minimal for Raspberry Pi).
# sqlite3, threading, http.server and tarfile come from the standard library.
opencv-python-headless==4.10.0.84
requests==2.32.3

# Dev/test only:
#   pip install pytest==8.3.4
